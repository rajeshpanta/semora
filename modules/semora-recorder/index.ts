// The JS face of this module lives in lib/lectureCapture/nativeEngine.ts, which
// loads it with requireOptionalNativeModule('SemoraRecorder') so that builds
// without it (everything before 1.15, Expo Go, web) keep working.
export {};
