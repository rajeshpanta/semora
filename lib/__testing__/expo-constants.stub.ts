/**
 * Test stub for expo-constants.
 *
 * lib/analytics.ts reads exactly one thing from it — the app version stamped
 * onto every event — so that is all this provides. Kept deliberately thin: a
 * stub that grows features the real module has but the tests never exercise
 * starts asserting things about a dependency instead of about our code.
 */
export default {
  expoConfig: { version: '0.0.0-test' },
};
